# 2026-07-03 — Stability and report consolidation

Stabilisation pass before adding more gameplay systems.

Changed:

- Current version is now `0.1.23-stability-report-consolidation`.
- Synced `prototype/README.md` and `CURRENT_STATUS.md` to the current milestone.
- Merged the useful patched Markdown export behaviour back into the canonical `prototype/src/systems/reportSystem.js`.
- Switched export users back to `reportSystem.js`.
- Removed obsolete `prototype/src/systems/reportSystemPatched.js`.
- Markdown export now keeps the useful daily review sections plus Special Requests, Pricing, Notebook, Condition Summary, Detailed Condition Changes, and Debug Log.
- Preserved the earlier fix that suppresses misleading "nothing is visible" display notes after stock has sold.
- Special request results are day-stamped.
- Request log is reset when a new order/day cycle begins, so request revenue cannot double-count across days.
- Trading auto-advance now waits while an active special request exists.
- Reducer now blocks `RUN_TRADING_CHECKPOINT` after the trading clock is complete.
- Expanded `npm run smoke` with version, capacity, display-zone, clock-boundary, report-section, and request-log reset invariants.

Still not added:

- staff
- upgrades
- save/load
- drag-and-drop display
- final art integration
- new plant systems
- new locations
- browser UI test framework

Notes:

- This is a stability pass, not a feature build.
- The smoke test remains headless and reducer/system-level.
